using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Part : MonoBehaviour
{
  /*  // Start is called before the first frame update
    private List <GameObject> HandList;
    private HandModel [] handModel;

    private Transform Spot;
   private string file = "Assets/Resources/Hands";
    //private int count = Directory.GetFiles(file).Length;
 private int currentHand = 0;
  
    void Start()
    {
        handModel = Resources.LoadAll<HandModel>("Hands");
        HandList = new List <GameObject>(); 
      
     for (int i = 0; i < handModel.GetLength; i++)
     {
         HandList.Add(handModel[i]);
     }
        
       
       
        foreach (GameObject go in HandList)
        {   go = Instantiate(handModel.handPrefab, Spot);
            go.SetActive(false);
        }
        HandList[currentHand].SetActive(true);

    }*/
    // Update is called once per frame
    void Update()
    {
        
    }
}
